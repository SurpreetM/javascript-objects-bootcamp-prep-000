var playlist = {Slowdive: "Alison"}

function updatePlaylist (obj, artistName, songTitle) {
 obj[artistName] = songTitle
 return obj
}

funtion removeFromPlaylist (obj, artistName) {
  
  return obj
}